#include<iostream>
#include<string>
#include<sstream>
#include<vector>

using namespace std;

void RWeight() {
	int X, N;
	cin >> X >> N;

	string W;
	getline(cin, W);
	stringstream ss(W);

	vector<int> array;
	int tmp;
	while (ss >> tmp) array.push_back(tmp);

	int Q;
	cin >> Q;

	int result = 0;
	for (int i = 0; i < Q; i++) {
		result = 
	}
	

	
}